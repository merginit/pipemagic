name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
      - run: yarn install --frozen-lockfile --ignore-engines

      # Build the runtime package (needed by both apps)
      - run: yarn build:runtime

      # Build the main Nuxt app
      - run: yarn generate
        env:
          NUXT_APP_BASE_URL: /pipemagic/

      # Build the example app into the Nuxt output
      - run: cd packages/example && npx vite build --base /pipemagic/example/ --outDir ../../.output/public/example

      - uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
